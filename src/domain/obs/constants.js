// The default REST representation to use when fetching an obs

export const DEFAULT_OBS_REP = '(id,uuid,display,obsDatetime,value:(id,uuid,display,name:(uuid,name)),encounter:(id,uuid,encounterDatetime))';
