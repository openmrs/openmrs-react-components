import types from 'redux-types';

import { BASIC_TYPES } from "../../types";
export const SET_CURRENT_LOCATION_PREFIX_SUCCEEDED = 'session/SET_CURRENT_LOCATION_PREFIX_SUCCEEDED';
export default types('session', BASIC_TYPES);

