import React from 'react';
import { Button } from 'react-bootstrap';

const Submit = (props) => {
  return (
    <Button
      bsStyle="success"
      type="submit"
    >
      Submit
    </Button>
  );
};

export default Submit;
